# Docker Configuration
TZ=America/Toronto

# MongoDB Atlas Configuration (for Docker)
DATABASE_URL=your-cluster.mongodb.net
DATABASE_NAME=your-database-name
DATABASE_USER=your-username
DATABASE_PASSWORD=your-password

# Container Ports
FRONTEND_PORT=3001
BACKEND_PORT=5002

# NOTE: For local development without Docker, create
# backend/.env with the same Atlas credentials
# And frontend/.env if needed.
# See frontend/.env.dist and backend/.env.dist for the format
